# Battery test run artifacts — generated, not source-controlled
server/*/kitchen-sink/battery_report_*.json

# Python bytecode
__pycache__/
*.pyc
*.pyo

# Local provider secrets — NEVER commit real keys
providers/secrets.env

# Local test-server run dir (jars, sqlite, logs)
.run/
